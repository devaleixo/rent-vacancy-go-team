<div class="card-product">
  <% @parking_lot.photos.each do |photo| %>
    <%= image_tag(photo)  %>
  <% end %>
    <div class="card-product-infos">
      <h2><%=@parking_lot.address%></h2>
      <p>R$: <strong><%=@parking_lot.price%></strong> only.</p>
      <p><%=@parking_lot.overview%></p>
    </div>
</div>
